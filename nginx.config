events {}

http {
    server {
        listen ${PORT};  # Use the environment variable PORT provided by Cloud Run
        root /usr/share/nginx/html;
        index index.html;

        location / {
            try_files $uri /index.html;
        }
    }
}
